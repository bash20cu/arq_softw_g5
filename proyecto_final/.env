SECRET_KEY=dev-secret-change-me

# SQL Server (Docker local via docker-compose.yml)
MSSQL_HOST=localhost
MSSQL_PORT=1433
MSSQL_DB=SistemaVentas
MSSQL_USER=sa
MSSQL_PASSWORD=TuPasswordFuerte123!
MSSQL_DRIVER=ODBC Driver 18 for SQL Server

# Enable only for first-time local setup (creates DB + runs schema/seed).
BOOTSTRAP_DATABASE=true

# PayPal (optional; only needed for real sandbox flow)
PAYPAL_BASE_URL=https://api-m.sandbox.paypal.com
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
